# 个人作业：简历数据探索性分析（EDA）

## 提交

- 提交链接：[点击提交作业](https://workspace.jianguoyun.com/inbox/collect/submit) 🍎
- 作业格式要求参见：[readme](https://github.com/arlionn/ds/blob/main/homework/readme.md)

---

## 数据说明

本次作业使用 **中文简历筛选数据集**（`Chinese_resume_data.csv`），共 5000 条记录，34 个字段，涵盖求职者的基本信息、教育背景、技能栈、工作经验、项目经历，以及最终筛选结果（`通过` / `不通过`）。数据为模拟生成，不含真实个人信息。

- **数据下载**：[点击查看](https://github.com/lianxhcn/ds_data/tree/main/Chinese_resume)

> 🍏 **温馨提示**：本作业预计需要 **1–3 小时** 完成，请尽早开始。

---

## 格式要求

针对每个小问题，请按如下顺序插入四个单元格：

1. **Markdown 块（分析目的）**：说明本小题的分析目的和思路，用自己的话阐述"我想了解什么、为什么要分析这个"。
2. **Markdown 块（提示词）**：写出你用来生成代码的提示词（Prompt），可使用 Copilot、ChatGPT 等工具辅助生成代码。提示词需用代码块包裹（` ```raw ... ``` `）。
3. **Code 块**：粘贴并运行 Python 代码，确保输出结果可见。
4. **Markdown 块（结果解读）**：对输出结果进行简要分析，重点回答"数据告诉了我们什么？"。

---

## 任务 A：数据加载与初步了解

### A1. 加载数据并查看基本信息

完成以下操作：
- 用 `pandas` 读取 `Chinese_resume_data.csv`，存入名为 `df` 的数据框。
- 呈现数据的 **形状**（行数、列数）、**列名列表** 和 **前 5 行**。
- 用 `df.info()` 输出各列的数据类型和非空值数量，识别哪些列存在缺失值。

**提示**：注意区分"真实缺失"（`NaN`）和数据中填写的 `"NULL"` 字符串，思考如何统一处理。

---

### A2. 缺失值分析与数据清洗

- 统计每列的缺失率（含字符串 `"NULL"` 视为缺失），用条形图可视化缺失情况。
- 将字符串 `"NULL"` 替换为 `NaN`，并再次统计缺失率，对比前后变化。
- 说明哪些列缺失严重（缺失率 > 50%），并讨论在后续分析中应如何处理。

---

### A3. 因变量分布

- 统计 `筛选结果`（通过 / 不通过）的频数和频率，用 **饼图** 和 **条形图** 分别呈现。
- 分析：两类样本是否均衡？样本不均衡对后续机器学习建模有何影响？

---

## 任务 B：单变量分析

### B1. 数值型变量的描述性统计

针对以下数值型变量，计算并呈现均值、中位数、标准差、最小值、最大值、偏度和峰度：
- `年龄`、`小规模项目`、`中规模项目`、`大规模项目`

**提示**：可使用 `df.describe()` 并手动补充偏度和峰度，或使用 `pd.DataFrame.agg()`。

---

### B2. 年龄分布

- 绘制 `年龄` 的 **直方图**（bins=20），并叠加 **核密度曲线**（KDE）。
- 分别绘制 **通过** 和 **不通过** 两组的年龄分布，置于同一图中对比。
- 分析：通过筛选的求职者在年龄上是否有明显特征？

---

### B3. 类别型变量的频率分析

分别绘制以下变量的 **条形图**（按频率降序排列）：
- `意向岗位`（10 类）
- `学历层次`
- `院校类别`
- `英语水平`

每幅图请在条形顶端标注百分比数值，并对图形结果做简要说明。

---

### B4. 工作经验变量的分布

数据集中工作经验以区间形式存储（如 `"1年以下"`、`"1―3年"`、`"3―5年"` 等），请：
- 统计 `小型企业工作经验`、`中型企业工作经验`、`大型企业工作经验` 各类别的频数。
- 将经验区间转换为数值（如取区间中点，`"1年以下"=0.5`，`"1―3年"=2`，`"3―5年"=4`，`"5年以上"=6`），新建三列 `小型_年`、`中型_年`、`大型_年`。
- 绘制三类企业经验的 **箱线图**（三组并排）。

---

## 任务 C：双变量与分组对比分析

### C1. 各意向岗位的筛选通过率

- 按 `意向岗位` 分组，计算每类岗位的 **通过率**（通过人数 / 该岗位总人数）。
- 用 **水平条形图** 呈现各岗位通过率，从高到低排序，并在条形末端标注百分比。
- 分析：哪类岗位最难通过？可能的原因是什么？

---

### C2. 学历与通过率的关系

- 交叉制表：以 `学历层次` 为行，`筛选结果` 为列，计算各组人数及通过率。
- 绘制 **堆叠条形图**（归一化），横轴为学历层次，颜色区分通过 / 不通过。
- 分析：学历对筛选结果有无显著影响？

---

### C3. 院校类别与通过率的关系

- 重复 C2 的分析，但将 `学历层次` 换为 `院校类别`。
- 对比 C2 和 C3 的结论：学历层次与院校类别，哪个对筛选结果的影响更大？

---

### C4. 编程语言熟练度与通过率

- 数据中 `编程语言熟练度` 包含多个值（用逗号分隔），请提取**第一门编程语言的熟练度**作为主要技能水平。
- 将熟练度（如"了解"、"掌握"、"熟练"、"精通"）映射为有序数值 1–4。
- 绘制 **箱线图**，横轴为通过 / 不通过，纵轴为熟练度数值，并叠加均值点。
- 分析：编程语言熟练度与通过率之间是否存在正相关？

---

### C5. 工作经验年限与通过率

- 使用 B4 中转换后的数值列（`小型_年`、`中型_年`、`大型_年`），计算三类经验年限与 `筛选结果`（0/1 编码）之间的 **相关系数**。
- 绘制 **散点图矩阵**（pair plot），颜色区分通过 / 不通过。
- 讨论：哪类工作经验与通过率的相关性最强？

---

### C6. 项目经验与通过率

- 计算 `小规模项目`、`中规模项目`、`大规模项目` 在通过组和不通过组的均值，用 **分组条形图** 呈现。
- 绘制 **热力图**，展示三类项目数量与筛选结果（0/1）的相关矩阵。
- 分析：大规模项目经验对通过率的贡献是否比小规模项目更大？

---

## 任务 D：多变量综合分析

### D1. 不同岗位的技能结构雷达图

选取数量最多的 **4 类意向岗位**（如数据分析师、后端开发工程师、前端开发工程师、测试工程师），对各岗位中 **通过** 的简历：
- 统计以下技能领域的 **非空率**（即拥有该技能的比例）：前端技术、后端技术、数据库、云计算/运维、数据与算法、移动开发、测试工具。
- 绘制 **雷达图**（每个岗位一条线），对比不同岗位的技能侧重。

**提示**：可使用 `matplotlib` 的极坐标图，或使用 `plotly` 绘制交互式雷达图。

---

### D2. 相关性热力图

- 将所有类别变量（学历层次、院校类别、英语水平、编程语言熟练度等）按合理方式编码为数值。
- 计算所有数值型特征（含编码后的类别特征）与 `筛选结果`（0/1）的相关系数矩阵。
- 用 **热力图** 呈现（注意调整字体大小和配色使其清晰易读）。
- 找出与 `筛选结果` 相关性绝对值最高的前 5 个特征，简要分析其含义。

---

### D3. 综合画像：通过 vs 不通过

请自行设计一张 **"简历通过 vs 不通过"综合对比图**，在单张图中同时展示至少 4 个维度的差异（例如：年龄、学历、工作经验、项目经历等）。

可参考的图形形式：多子图布局、分组条形图、小提琴图组合等。要求图形清晰、美观，有标题和必要的图例。

---

## 提交说明

1. 新建一个 `ex_P03_姓名.ipynb` 文件。
2. 第一个单元格为 Markdown，包含：**姓名、学号、作业简介**（2–3 句话说明本次 EDA 的主要发现）。
3. 每个任务对应的四个单元格格式严格按照上文要求。
4. 所有图形需设置 **中文字体**（推荐 `matplotlib` 配合 `matplotlib.rcParams['font.family'] = 'SimHei'` 或使用 `zhplot` 库），避免出现乱码。
5. 最终提交 `.ipynb` 文件，确保所有单元格已运行并有输出。
